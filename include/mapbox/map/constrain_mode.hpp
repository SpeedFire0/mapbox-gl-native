#pragma once

#include <mapbox/common/config.hpp>

namespace mapbox {
namespace map {

enum class ConstrainMode {
    None,
    HeightOnly,
    WidthAndHeight
};

} // namespace map
} // namespace mapbox
